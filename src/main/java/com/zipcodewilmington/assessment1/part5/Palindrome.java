package com.zipcodewilmington.assessment1.part5;

public class Palindrome {

    public Integer countPalindromes(String input){
        /*char[] stringToArray = input.toCharArray();
        //char[] reverse = new char[0];
        boolean result = true;
        int index2 = input.length()-1;
        for (int index = 0; index <= stringToArray.length-1; index++){

            if(!input[index].equals(array[index2])){

                result = false;

            }
            index2--;
        }
*/        return null;
    }
}
